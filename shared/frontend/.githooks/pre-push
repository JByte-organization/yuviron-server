#!/usr/bin/env bash
set -Eeuo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

echo "[pre-push] Running frontend checks before push..."
bash "$PROJECT_ROOT/scripts/check-frontend-fast.sh"
echo "[pre-push] All checks passed"